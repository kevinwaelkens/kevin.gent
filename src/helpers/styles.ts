import colors from './colors';

const styles = {
    navigationBackground: colors.black,
    navigationLink: colors.white,
    imageControls: colors.white,
    link: colors.black,
};

export default styles;
